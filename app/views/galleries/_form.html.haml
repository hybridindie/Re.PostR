= form_for(@gallery) do |f|
  - if @gallery.errors.any?
    #errorExplanation
      %h2= "#{pluralize(@gallery.errors.count, 'error')} prohibited this user from being saved:"
      %ul
      - @gallery.errors.full_messages.each do |msg|
        %li= msg
  

  .actions
    = f.submit
